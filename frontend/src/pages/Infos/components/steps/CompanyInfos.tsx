import { register } from 'react-scroll/modules/mixins/scroller';
import Input from '../Input';

const CompanyInfos = () => {
  return (
    <div>
      <Input name="companyName" placeholder="Company Name" type="string" />

      <Input name="companyName" placeholder="Company Name" type="string" />

      <Input name="companyName" placeholder="Company Name" type="string" />
    </div>
  );
};

export default CompanyInfos;
